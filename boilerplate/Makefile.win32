
SUBMAKEFILE = 1

include ../Makefile.win32

CFLAGS += -I../src

SOURCES = \
	cairo-boilerplate.c \
	xmalloc.c \
	$(NULL)

OBJECTS = $(subst .c,.obj,$(SOURCES))

all: boiler.lib

boiler.lib: $(OBJECTS)
	lib -NOLOGO -OUT:$@ $(OBJECTS)

